# Shower Agent MVP — 功能、流程与实现效果说明

用于后续检测与优化的统一描述。

---

## 一、软件定位与作用

**名称**：Shower Agent MVP（手持花洒评价图套组生成器）

**核心作用**：  
用户上传产品图、场景图、模特图、出水图等素材后，系统按**固定场景与人物锁定**的规范，生成一套可用于「买家秀/评价图」的 **Shot1 锚点图 + Shot2–Shot7 共 6 张交付图** 的**执行级 Prompt**，并预留图片占位与修改入口，为后续接入真实图像生成 API（如 Banana Pro）做准备。

**解决的问题**：  
- 避免手写 Prompt 时漏掉参考图锁定、十段式结构，导致生成图换人换景、风格不一。  
- 统一「参考图 1 / A / B / water」的语义，保证同一套内人物、场景、产品外观一致。

---

## 二、主要功能

| 功能模块 | 说明 |
|----------|------|
| **上传素材** | 按槽位上传：产品图、场景图、模特图（可选）、出水效果图（至少 1 张）、便携包图（TravelFilter）、滤芯图（Filter）。支持多张出水图，UI 为两排网格展示。 |
| **花洒类型** | 选择 Boost / LargePanel / Filter / TravelFilter / Descale，决定使用的 Skill 与是否必传便携包/滤芯。 |
| **Shot1 锚点** | 展示 Shot1 占位图；旁侧展示「Shot1 生成用 Prompt」文本（当前为前端模板，主体产品锁定参考图1）。支持「重新生成 Shot1 Anchor」（mock 刷新）。 |
| **确认并生成 Shot2–Shot7** | 确认 Shot1 后，调用后端生成 6 条 V3.2 执行级 Prompt（8 选 6 镜头类型、至少 2 张出水等），返回 6 张占位图 + prompts_cn / prompts_en（dict）。 |
| **Shot2–Shot7 展示与编辑** | 左侧 60% 为 6 张卡片（含「已修改」角标、「重生成本 Shot」按钮）；右侧 40% 为修改区：修改指令（大文本框）、中文文案编辑、局部重生成、保存到本地。 |
| **全套重生成** | 再次调用 `/api/generate/shot2to7`，覆盖 6 张的 prompt 与占位图。 |
| **单张重生成** | 卡片上「重生成本 Shot」或右侧「局部重生成」，调用 `/api/modify/shot`（instruction 可为 `regenerate`），返回新占位图。 |
| **导出** | 下载 JSON、prompt.txt、images_used.txt，便于对接下游或留档。 |

---

## 三、用户操作流程（端到端）

1. **选择花洒类型**（若未上传则先选类型再上传）。  
2. **上传素材**  
   - 第一排：产品图、场景图、模特图。  
   - 第二排：出水效果图、便携包（TravelFilter 必填）、滤芯（Filter 必填）。  
   - 点击「上传」→ 后端创建 session，返回 session_id。  
3. **锚点区**  
   - 查看 Shot1 占位图及旁侧「Shot1 生成用 Prompt」文本。  
   - 可选「重新生成 Shot1 Anchor」（仅 mock）。  
   - 点击「确认生成 Shot2–Shot7」。  
4. **生成结果区**  
   - 展示 6 张 Shot2–Shot7 卡片；可点击卡片在右侧查看/编辑该 Shot 的修改指令与中文文案。  
   - 可「全套重生成」或单张「重生成本 Shot」/「局部重生成」。  
   - 可导出 JSON、prompt.txt、images_used.txt。  
5. **返回重新上传**：清空当前 session，回到上传步骤。

---

## 四、后端逻辑要点（与检测相关）

- **Session**：上传创建 session，确认 Shot1 后 `shot1_confirmed = true`，生成 Shot2–7 与修改单张均依赖同一 session。  
- **参考图与选图**：`role_map` 由上传文件名解析（如 `product__0__xx.jpg`）；`select_images(role_map, flags, mode)` 决定送入模型的图片列表；当前为 S1 模式（product/scene/water 各一 + model 一）。  
- **Shot2–Shot7 Prompt**：`prompt_v32.build_shot2to7_prompts_v32(role_map, flags)` 生成 6 条中文/英文 Prompt，规则包括：  
  - 参考图锁定（Shot2–7 主体产品结构+外观均为参考图A；场景与人物为参考图B；出水为 water）。  
  - 10 段式结构（产品动作、主体产品、人物特征、人物姿态、人物装饰与服饰、场景、场景装饰与道具、摄像机角度、焦距与光圈、灯光、摄影风格与品质）。  
  - 8 选 6 镜头类型（④ 必出、至少 2 张出水、⑥ 最多 1 张等）；仅「产品动作、摄像机角度、焦距与光圈」等按镜头变化；人物/场景等仅标注「（参考图B锁定）」或留空，待接入 API 后从 Shot1 继承。  
- **图片与生成**：当前为 mock 占位图 URL；真实出图未接入，仅返回 URL 与 Prompt，供后续接 Banana Pro 等。

---

## 五、最终实现效果（可验收点）

| 验收项 | 说明 |
|--------|------|
| 上传与 session | 按槽位上传后能创建 session，且后续步骤均带同一 session_id。 |
| Shot1 展示 | 锚点区有 Shot1 图 + 旁侧「Shot1 生成用 Prompt」文本（参考图1 锁定、十段式结构）。 |
| Shot2–Shot7 生成 | 确认后得到 6 张卡片，每张对应一条完整 10 段式 Prompt，顶部含【参考图锁定】段，且 prompts_cn/en 为 dict（shot2…shot7）。 |
| 镜头与约束 | 6 张中至少 2 张出水、④ 必出、⑥ 最多 1 张；产品动作/镜头/焦距随类型变化，人物场景不换。 |
| 修改与重生成 | 可改修改指令与中文文案并「局部重生成」；可单张「重生成本 Shot」或「全套重生成」；保存到本地后卡片有「已修改」角标。 |
| 导出 | 可下载 JSON、prompt.txt、images_used.txt，内容与当前 session 及 6 条 Prompt 一致。 |

---

## 六、当前限制与后续优化方向

- **Shot1 与人物/场景文案**：Shot1 的 Prompt 为前端模板；Shot2–7 的人物特征、人物姿态、场景等仅锁定标注或留空，**未接入真实生成 API**，故无「从生成结果继承」的完整文案；接入 API 后需用 Shot1 的生成结果填充上述字段。  
- **图片为占位**：所有展示图片均为占位 URL，真实出图需对接图像生成服务。  
- **CORS / 接口**：前端请求 `http://127.0.0.1:8000`，需后端与前端同时启动方可完整跑通流程。

以上可作为你进行**功能检测、回归测试和优化**的基准说明；若有具体检测用例或优化目标，可在此文档上补充章节。
